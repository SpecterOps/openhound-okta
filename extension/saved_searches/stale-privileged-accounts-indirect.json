{
  "name": "Okta: Stale Privileged Users (Indirect)",
  "description": "Finds user accounts that have not logged in for at least 180 days and hold privileged role assignments through group membership.",
  "query": "MATCH path = (user:Okta_User)-[:Okta_MemberOf]->(:Okta_Group)-[:Okta_HasRoleAssignment]->(:Okta_RoleAssignment)-[:Okta_ScopedTo]->(:Okta)\nWHERE user.lastLogin IS NULL OR datetime(user.lastLogin) <= datetime() - duration(\"P180D\")\nRETURN path\nLIMIT 1000"
}
